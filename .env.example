# Supabase (required)
VITE_SUPABASE_URL=https://zirftywinscopzuuwdlg.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key

# Web push — public key only (generate: npm run generate:vapid-keys)
# Must match VAPID_PUBLIC_KEY in Supabase Edge Function secrets.
VITE_VAPID_PUBLIC_KEY=

# Email notifications via n8n (optional)
VITE_N8N_NOTIFICATION_WEBHOOK_URL=
VITE_N8N_NOTIFICATION_WEBHOOK_SECRET=
